name = "benchmark-hub-ai"
main = "src/index.js"
compatibility_date = "2024-06-01"
compatibility_flags = ["nodejs_compat"]

# Bind Cloudflare Workers AI
[ai]
binding = "AI"

# CORS origins — update with your Cloudflare Pages domain after deploy
[vars]
ALLOWED_ORIGIN = "*"
